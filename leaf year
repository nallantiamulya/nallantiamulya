#include<stdio.h>
void main()
{
    int n;
    printf("Enter year:");
    scanf("%d",&n);
    if(n%100==0 && n%400==0 || n%4==0 && n%100!=0)
        {
            printf("given year is a leaf year");
        }
    else
    {
       printf("Given year is not a leaf year");
   }
}
