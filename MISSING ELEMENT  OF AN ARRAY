#include<stdio.h>
void main()
{
    int a[20];
    int l;
    printf("enter length of array");
    scanf("%d",&l);
    printf("enter %d no of elements",1);
    for(int i=0;i<1;i++)
    {
        scanf("%d",&a[i]);
    }
    int max=a[0];
    int temp;
    for(int j=1;j<1;j++)
     {
         max=a[j];
     }
}
